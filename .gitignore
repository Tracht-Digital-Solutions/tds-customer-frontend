node_modules/
dist/

# The deployable tree scripts/pack-release.mjs assembles from dist/.
# CI publishes it to the dev/release branch; it is never committed here.
release/
.astro/
*.log
.DS_Store
.vscode/
.idea/
*.tsbuildinfo

# Windows-generated npm lockfile is win32-only; CI uses npm install --no-package-lock
package-lock.json

Thumbs.db
Desktop.ini
*.swp
*~

# Local build environment (see .env.example). Never commit a real one.
.env
.env.production
